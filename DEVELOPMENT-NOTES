//RECENT CHANGES

# Labeling now works.

# Formatting/Labeling from files.

# Change default label.

# Some pretty stupid memory allocation errors in process.c payload.c have been fixed.

# seq/seq_high in classification_tree_node are now unisigned long long.

//CURRENTLY WORKING ON

# Generating ARFF headers.

# Finishing wait-for-args arguments, so that GUI scripts can interface with the program
